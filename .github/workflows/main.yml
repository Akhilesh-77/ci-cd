name: Build, Test, and Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # or your preferred version

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Build project
        run: echo "Building project..." # Replace with actual build command if needed

      - name: Deploy (example)
        if: github.ref == 'refs/heads/main'
        run: |
          echo "Deploying to server or platform..."
          # Example for static deployment
          # npm run deploy
          # or use scp/rsync/aws/github pages/etc.
